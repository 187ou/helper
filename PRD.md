# 自适应演化个人工作台 PRD完整需求清单（适配 React+Vite+TailwindCSS 前端栈｜含优先级+验收标准）
> ✅ 改动说明：
> 1. 保留原有业务需求、P0/P1/P2 优先级、验收逻辑不变；
> 2. **新增/改写【前端落地细则】**，贴合 React+Vite+Tailwind、组件化、状态管理、响应式、虚拟滚动、看板DAG画布等技术选型；
> 3. 区分「业务落地细则」&「前端专项落地细则」，方便前端直接认领开发；
> 4. 适配 Electron（因为你要打包成EXE桌面端），兼容本地文件系统、托盘、Ollama本地通信；
> 5. 去掉不适合React单页实现的粗粒度描述，补充组件、路由、状态、样式方案；
> 6. 通用验收规则增加前端专项验收项。

## 文档说明
1. 优先级定义
- **P0（首发必做）**：V1.0 上线核心功能，无则产品不成立，覆盖工作台底座、演化核心、办公生活基础场景、私有化隐私能力
- **P1（迭代增强）**：V1.1 迭代功能，完善体验、补齐场景、强化产品差异化，不阻塞首发
- **P2（中长期规划）**：V2.0+ 扩展功能，预留架构接口，本次版本不开发

2. **通用验收规则（新增前端专项）**
所有功能**纯本地私有化运行、数据不上传云端**，打包为 Electron + React+Vite+TailwindCSS，支持Windows10/11，适配联网/离线Ollama双模式，EXE单文件可直接运行。
> 前端通用验收基线：
> - 采用TailwindCSS实现样式，禁止大量自定义css；组件基于函数式React，hooks管理状态；
> - 路由基于React Router；复杂状态使用Zustand/Context（轻量，不引入Redux重方案）；
> - 大列表（任务、日志、知识库文档）使用虚拟滚动，防止DOM爆炸；
> - 布局自适应缩放，支持窗口resize，无横向滚动溢出；
> - 支持暗黑/浅色主题（Tailwind内置）；
> - Electron主线程隔离文件IO、托盘、本地数据库通信；渲染层React只负责UI和交互。

---

# 一、工作台全局中枢模块（P0 核心底座｜React SPA + 可视化看板）
### 1.1 全局可视化任务看板
**优先级**：P0
**功能描述**：搭建工作台核心看板，聚合工作、生活、健康全量待办，支持多视图切换，直观展示每日/每周事务全貌
**业务落地细则**：默认今日视图，支持本周/清单/时间线视图切换；区分工作待办、生活待办、健康提醒三大卡片；展示任务完成率、待办总数、已完成统计
**【前端专项落地细则】**
- 路由：`/dashboard` 首页看板；视图切换使用状态驱动（today / week / list / timeline）
- 组件拆分：`DashboardLayout`、`TaskStatCard`、`TaskGroupCard`、`TimelineView`
- Tailwind：采用grid/flex弹性布局，卡片自适应，窗口resize自动重排；支持暗色模式
- 统计数字使用`useMemo`缓存计算，避免重复渲染；任务列表开启虚拟滚动
**验收标准**：
1. 正常加载全量任务数据，无缺失、无错乱；
2. 视图切换流畅，数据实时同步；
3. 完成率统计精准；
4. 适配窗口缩放，布局自适应，无溢出错乱；
5. 前端视图切换无白屏、无不必要重渲染。

### 1.2 全生命周期任务管理
**优先级**：P0
**功能描述**：统一收纳手动新建、AI生成、定时触发、文档提取的所有任务，实现全状态、全维度管理
**业务落地细则**：任务状态流转：待办→进行中→已完成→归档/搁置；支持自定义标签、优先级（高/中/低）、截止时间、关联本地文档；支持手动增删、修改、标记完成
**【前端专项落地细则】**
- 组件：`TaskItem`、`TaskDetailDrawer`（抽屉弹窗，不新开页面）、`TaskTag`、`PriorityBadge`
- 表单使用受控组件；编辑/新建任务用React Hook Form + Tailwind表单样式
- 任务变更通过Zustand全局状态同步，跨视图实时更新；关联文档点击唤起Electron本地打开
**验收标准**：
1. 所有任务状态可正常切换、数据实时入库；
2. 标签、优先级、截止时间可自定义保存；
3. 支持任务关联多份本地文档，点击可快速打开；
4. 抽屉表单打开关闭流畅，表单数据双向绑定正常。

### 1.3 DAG任务可视化编排
**优先级**：P0
**功能描述**：基于LangGraph动态生成任务链路流程图，可视化展示串行/并行节点、执行进度，支撑自演化闭环
**业务落地细则**：自动拆解混合任务生成DAG流程图；节点区分未执行/进行中/成功/失败四种状态；支持任务断点续跑、失败重试、进度实时刷新
**【前端专项落地细则】**
- 选用轻量React流程图库（如`reactflow`，适配Vite）渲染DAG；
- 节点组件封装`DagNode`，用Tailwind做状态色（默认/进行中/成功/失败）
- 画布支持缩放、拖拽平移；节点点击打开任务详情；执行进度用websocket/轮询更新
- 流程图数据为JSON，由后端/主进程LangGraph输出，ReactFlow直接消费
**验收标准**：
1. 复杂混合任务可自动拆分合规DAG链路；
2. 节点状态同步准确，进度实时更新；
3. 失败节点标红，支持手动重试；
4. 断点续跑生效，无需重新执行全流程；
5. 画布缩放、拖拽流畅，大量节点不卡顿。

### 1.4 全局快捷能力
**优先级**：P0
**功能描述**：提供悬浮快捷输入、全局检索、托盘常驻能力，提升工作台操作效率
**业务落地细则**：悬浮窗可拖拽、置顶、快捷下发指令；右下角托盘常驻，支持右键唤醒主窗口、切换运行模式、开机自启设置；全局检索支持任务、文档、记账、演化日志全文搜索
**【前端专项落地细则】**
- 悬浮快捷框：独立React组件，Electron无边框子窗口 + 拖拽逻辑；置顶由Electron控制
- 全局搜索框：`SearchInput`组件，防抖输入（300ms），结果弹窗浮层
- 托盘菜单逻辑在Electron主进程；React渲染层提供配置面板映射托盘选项
- 检索结果虚拟滚动渲染，按类型分组（任务/文档/日志）
**验收标准**：
1. 悬浮窗正常显示、拖拽、置顶生效；
2. 托盘后台常驻，最小化不退出程序；
3. 全局检索精准匹配对应内容，响应迅速；
4. 搜索防抖生效，输入不频繁请求。

### 1.5 定时自动事务调度
**优先级**：P0
**功能描述**：贴合上班族作息，自动执行每日、周期性固定事务
**业务落地细则**：早8点自动推送当日工作清单；下班自动归档当日资料；月末自动汇总月度工作数据；定时触发健康提醒、记账复盘提醒
**【前端专项落地细则】**
- 页面：`/settings/schedule` 定时规则配置页
- 组件：`ScheduleRuleItem`、Cron简易选择器；表单受控保存
- 定时器逻辑放Electron主进程（渲染层React不承担定时，避免页面休眠失效）；React只做配置展示、提醒弹窗
- 提醒弹窗使用Tailwind浮层toast/modal
**验收标准**：
1. 所有定时任务准时触发，无遗漏、无提前延迟；
2. 自动归档、数据汇总完整规范；
3. 定时规则可手动开启/关闭、自定义时间；
4. 配置页增删改规则实时生效，重启程序保留。

---

# 二、核心壁垒：自适应演化引擎（P0 产品差异化核心）
### 2.1 用户行为全量采集
**优先级**：P0
**功能描述**：全程静默采集用户任务操作、人工修正、执行偏好数据，为自演化提供数据源
**业务落地细则**：记录用户原始指令、任务拆解步骤、执行耗时、手动修改内容、驳回操作、功能使用频次；所有数据本地私密存储，无云端上传
**【前端专项落地细则】**
- React侧埋点封装统一hook `useBehaviorTrack`；只捕获交互事件，不抓取键盘隐私输入
- 行为数据通过IPC传给Electron主进程写入SQLite，**前端不持久化**
- 可在设置页开关采集；开关状态全局响应式
**验收标准**：
1. 所有用户操作行为完整记录入库；
2. 无数据遗漏、无重复记录；
3. 数据仅本地留存，可手动清空；
4. 关闭采集后不再上报任何行为日志。

### 2.2 双维度演化裁判打分系统
**优先级**：P0
**功能描述**：自主评判任务执行效果，输出工作、生活双维度评分，作为迭代优化依据
**业务落地细则**：工作侧：数据准确率、文书完整度、材料规范度、执行耗时打分；生活侧：统计精准度、计划贴合度、归档完整度打分；评分区间0-100分，自动生成打分小结
**【前端专项落地细则】**
- 任务详情页、演化中心内展示分数卡片；`ScoreBadge`、`ScoreProgress`（Tailwind进度条）
- 打分小结使用Markdown渲染（`react-markdown`）
**验收标准**：
1. 每次任务完成后自动打分，评分维度贴合场景；
2. 打分结果可在演化中心、任务详情页查看；
3. 人工修正后可二次更新评分；
4. 分数可视化展示清晰。

### 2.3 工作流自动精简优化
**优先级**：P0
**功能描述**：自动识别任务流程冗余步骤，合并重复环节、优化串行并行逻辑，持续提升执行效率
**业务落地细则**：多次复用的同类任务，自动删减冗余步骤；固定时间节点的任务（如周三周报）自动预适配时间流程；耗时步骤自动优化为并行执行
**【前端专项落地细则】**
- 在进化中心展示优化前后DAG对比（双栏ReactFlow画布）
- 优化记录卡片使用折叠面板，默认收起详情
**验收标准**：
1. 高频同类任务可自动精简流程、缩短执行耗时；
2. 时间适配优化生效，贴合用户固定作息；
3. 流程优化记录同步录入演化日志；
4. 前后版本DAG可视化对比正常渲染。

### 2.4 个人习惯权重自适应迭代
**优先级**：P0
**功能描述**：动态调整用户习惯权重，高频偏好拉高优先级，过期无效习惯自动降权衰减
**业务落地细则**：日常作息、办公偏好、消费习惯等高频行为权重递增；往年旧数据、废弃流程、长期不用习惯权重递减；用户手动修正偏好永久留存生效
**【前端专项落地细则】**
- 演化中心增加权重视图：权重条目+Tailwind进度条可视化
- 权重明细支持折叠/展开
**验收标准**：
1. 权重动态更新精准，高频行为优先适配；
2. 过期数据自动降权，不干扰新任务执行；
3. 权重变化记录可在进化中心可视化查看。

### 2.5 高频工作流模板自动固化
**优先级**：P0
**功能描述**：自动沉淀用户高频混合任务流程，生成专属可复用模板，支持一键启动
**业务落地细则**：识别月末复盘+月度总结、报销整理+票据归档等混合高频场景；自动固化完整任务链路，生成模板；支持用户手动命名、保存、锁定模板，禁止自动覆盖
**【前端专项落地细则】**
- 路由：`/workflow-template`；卡片式列表展示模板（Tailwind grid）
- 模板详情页复用ReactFlow渲染DAG；「锁定」用Toggle开关组件
- 一键启动按钮唤起任务确认弹窗
**验收标准**：
1. 高频混合任务可自动生成专属模板；
2. 模板支持一键启动全流程；
3. 手动锁定后系统无法自动修改，保护用户自定义配置；
4. 模板列表虚拟滚动，大量模板不卡顿。

### 2.6 沙箱专属工具自动生成
**优先级**：P1
**功能描述**：基于用户高频重复操作，自主生成轻量化专用工具，沙箱安全测试后永久入库
**业务落地细则**：针对餐饮通勤记账、就医单据整理、批量文件归类等高频场景生成专属脚本；所有工具在本地沙箱隔离运行，无外部权限；新增工具自动纳入个人工具库
**【前端专项落地细则】**
- 工具库页面 `/tools`；卡片展示工具、描述、运行按钮
- 运行日志使用折叠日志面板，代码块用`react-syntax-highlighter` + Tailwind样式
**验收标准**：
1. 高频场景可自动生成对应工具；
2. 沙箱运行安全无风险，无文件篡改、无网络泄露；
3. 新增工具可手动调用、查看、删除。

### 2.7 进化中心可视化日志
**优先级**：P0
**功能描述**：时间线可视化展示工作台所有自演化记录，清晰呈现优化前后差异
**业务落地细则**：按流程优化、权重迭代、模板固化、工具新增分类展示；每条记录包含优化前内容、优化后内容、触发时间、优化原因；支持筛选、回溯、查看历史版本
**【前端专项落地细则】**
- 路由：`/evolution-log`；时间线布局用Tailwind实现，不引入重型时间线库
- 顶部筛选栏：类型下拉、时间范围筛选；日志列表虚拟滚动
- 每条日志可展开详情，DAG变更嵌入ReactFlow迷你画布
**验收标准**：
1. 所有演化行为完整记录展示；
2. 分类筛选精准，时间线排序正常；
3. 版本对比清晰，可完整回溯迭代过程；
4. 长日志滚动流畅。

### 2.8 演化自定义控制开关
**优先级**：P1
**功能描述**：支持用户自主控制演化节奏，可冻结演化、重置记忆、关闭自动优化
**业务落地细则**：提供演化暂停开关，暂停后系统不再自动迭代；支持单独清空权重记忆、模板记录、工具库；支持恢复默认演化规则
**【前端专项落地细则】**
- 配置页 `/settings/evolution`；使用Toggle、danger类型确认弹窗（Tailwind红色警示）
- 重置操作增加二次确认，防止误触
**验收标准**：
1. 开关生效精准，暂停后无自动优化行为；
2. 局部重置、全局重置功能正常；
3. 重置后不影响原有任务、文档数据；
4. 高危操作二次确认弹窗正常。

---

# 三、职场办公模块（P0 核心业务场景）
### 3.1 全周期文书自动撰写优化
**优先级**：P0
**功能描述**：自动生成日报/周报/月报/项目总结，支持公文润色、文风自适应
**业务落地细则**：自动读取当日/当月任务、台账数据填充文书内容；根据用户过往修改习惯自适应精简/详细文风；支持会议纪要自动提炼重点、拆分待办
**【前端专项落地细则】**
- 内置Markdown编辑器预览双栏；输出预览用`react-markdown`
- 生成过程中展示loading骨架屏（Tailwind skeleton）；输出后支持复制、保存本地
**验收标准**：
1. 各类周期文书生成完整、逻辑通顺；
2. 文风可自适应用户偏好；
3. 会议纪要重点清晰、待办事项完整拆分；
4. 双栏预览实时渲染无格式错乱。

### 3.2 Excel数据台账自动化处理
**优先级**：P0
**功能描述**：实现办公数据核算、表格处理、异常排查、可视化图表生成
**业务落地细则**：支持营收、费用核算，跨期数据对比；Excel批量合并、拆分、格式规整；自动识别数据异常并标注；基于数据表自动生成可视化图表+文字分析
**【前端专项落地细则】**
- 文件上传使用原生input封装；表格预览用`@tanstack/react-table` + Tailwind
- 图表使用`recharts`（React友好，Vite适配）；异常标红单元格用自定义cell渲染
**验收标准**：
1. 数据核算精准无误差；
2. 表格批量处理无错乱、无数据丢失；
3. 异常数据精准识别标注；
4. 图表生成规范、分析内容贴合数据；
5. 大数据表格支持虚拟滚动。

### 3.3 报销票据&行政事务整理
**优先级**：P0
**功能描述**：自动化整理报销材料、票据归档、请假事务梳理
**业务落地细则**：自动归集PDF/图片票据，整理报销明细清单；梳理请假事由、流程备注；规范报销材料格式，适配通用企业报销要求
**【前端专项落地细则】**
- 票据卡片网格布局；图片预览用lightbox浮层；明细表格复用Tanstack Table
- OCR识别中展示loading；识别结果可手动编辑修正
**验收标准**：
1. 票据归集完整、明细统计准确；
2. 报销材料格式规范、可直接使用；
3. 行政事务梳理清晰、信息完整；
4. 图片票据预览流畅。

### 3.4 本地文件智能归档管理
**优先级**：P0
**功能描述**：桌面文件自动归类、项目资料归档、文档批量处理、PDF研读解析
**业务落地细则**：自动规整桌面杂乱文件，按类型分类存档；按项目维度归集全套资料；支持文档批量重命名、格式统一；PDF重点提取、内容汇总、关键词检索
**【前端专项落地细则】**
- 文件浏览器组件：树状目录（递归React组件）；PDF预览使用`react-pdf`
- 批量操作多选框，顶部批量操作栏
**验收标准**：
1. 文件归类规整无错乱；
2. 项目资料归集完整、可快速检索；
3. 批量处理功能稳定无报错；
4. PDF解析提取内容精准；
5. 文件树展开收起流畅。

### 3.5 项目简易进度管控
**优先级**：P1
**功能描述**：轻量化管理个人项目，跟踪里程碑、子任务进度
**业务落地细则**：支持新建项目、拆分里程碑、添加子任务；实时统计项目完成率；关联项目对应文档、台账、会议记录
**【前端专项落地细则】**
- `/project` 项目列表；项目详情内里程碑+子任务清单；进度使用Recharts环形图
**验收标准**：
1. 项目、里程碑、子任务可正常新增编辑；
2. 进度统计精准；
3. 可关联对应项目资料，一键查看。

---

# 四、个人生活&健康&事务模块（P0 全覆盖场景）
### 4.1 精细化收支记账&复盘
**优先级**：P0
**功能描述**：手动/自动记账，跨期收支对比、月度消费复盘、数据统计可视化
**业务落地细则**：支持手动录入收支、票据OCR自动记账；分类统计餐饮、通勤、购物、生活支出；自动生成月度收支对比、消费小结；留存历史账单数据
**【前端专项落地细则】**
- `/ledger`；收支表单受控组件；账单列表虚拟滚动；收支趋势用Recharts
**验收标准**：
1. 记账数据精准，分类清晰；
2. 跨月对比、复盘分析准确；
3. 账单数据本地留存，可随时查询导出；
4. 图表随账单实时刷新。

### 4.2 全场景日程计划管理
**优先级**：P0
**功能描述**：覆盖作息、通勤、健身、备考、出游全场景计划排布
**业务落地细则**：支持单次/周期性日程创建；自动拆解学习、健身长期计划为每日任务；规划通勤、周末出游、居家事务；日程自动同步至看板展示
**【前端专项落地细则】**
- 简易日历组件（基于headlessui + Tailwind，不引入重型fullcalendar）；周期选择器
- 日程变更自动同步Dashboard看板状态
**验收标准**：
1. 各类日程可正常创建、编辑、提醒；
2. 长期计划拆解合理、贴合日常作息；
3. 看板日程展示同步无偏差。

### 4.3 职场健康智能提醒
**优先级**：P0
**功能描述**：针对上班族亚健康问题，提供久坐、喝水、作息、睡眠全维度管理
**业务落地细则**：自定义久坐休息、定时喝水提醒；自动记录每日作息、睡眠时长；留存个人饮食忌口、健康偏好；定时弹窗提醒，支持延后/关闭
**【前端专项落地细则】**
- `/settings/health` 配置页；提醒toast/modal由Electron主进程调度触发
**验收标准**：
1. 定时提醒准时触发，弹窗样式轻量化；
2. 健康数据记录完整、统计准确；
3. 偏好设置永久留存，可随时修改。

### 4.4 个人资料&杂项事务归档
**优先级**：P0
**功能描述**：统一收纳证件、账单、快递、笔记、就医单据等个人零散资料
**业务落地细则**：分类归档证件扫描件、缴费账单、就医票据；整理快递信息、阅读笔记、日常琐事记录；支持资料检索、预览、本地打开
**【前端专项落地细则】**
- 资料库网格卡片；分类侧边栏；图片预览浮层；搜索防抖
**验收标准**：
1. 资料分类规整、索引清晰；
2. 检索快速精准；
3. 所有私密资料本地留存，无泄露风险。

### 4.5 目标打卡&习惯养成
**优先级**：P1
**功能描述**：支持个人目标设定、日常习惯打卡、完成率统计
**业务落地细则**：新增月度/周度个人目标；每日习惯打卡签到；自动统计连续打卡天数、目标完成率；生成习惯养成小结
**【前端专项落地细则】**
- `/habit`；打卡日历视图；连续天数用徽章展示；完成率Recharts
**验收标准**：
1. 打卡功能正常，记录完整；
2. 完成率、连续天数统计精准；
3. 目标进度可视化展示清晰。

---

# 五、私有知识库资产模块（P0 核心资产底座）
### 5.1 本地私有化向量知识库
**优先级**：P0
**功能描述**：基于Chroma搭建纯本地向量库，实现私有文档存储、向量化、智能检索
**业务落地细则**：支持PDF/Word/TXT/Markdown/Excel/图片OCR文档上传；自动分片向量化；按工作合同、票据、笔记、生活资料分类归档；混合语义检索+时间权重检索
**【前端专项落地细则】**
- `/knowledge`；文档上传区、分类侧边栏、检索结果列表虚拟滚动
- 向量化中展示进度条；检索结果高亮匹配片段
**验收标准**：
1. 多格式文档可正常上传解析；
2. 向量化成功，检索精准；
3. 所有向量数据本地存储，无云端上传；
4. 检索高亮正常渲染。

### 5.2 富文本笔记编辑
**优先级**：P0
**功能描述**：内置轻量化笔记编辑器，支持内容编辑、附件嵌入、任务关联
**业务落地细则**：支持富文本/Markdown编辑；可嵌入本地文件、任务链接；笔记自动归类、留存版本记录
**【前端专项落地细则】**
- 轻量Markdown编辑器（如`react-markdown` + simpleMDE）；双栏编辑预览
- 附件拖拽上传；任务链接点击跳转对应任务详情
**验收标准**：
1. 编辑功能流畅，内容实时保存；
2. 支持附件、链接嵌入；
3. 笔记分类清晰、可快速检索。

### 5.3 文档智能解析&摘要生成
**优先级**：P1
**功能描述**：自动提炼长文档重点、生成摘要、标注核心信息
**业务落地细则**：针对长报告、合同、学习资料自动精简内容、提炼核心要点；生成结构化摘要，支持一键保存为笔记
**【前端专项落地细则】**
- 摘要结果Markdown渲染；一键保存按钮直接新建笔记
**验收标准**：
1. 摘要提炼精准，无核心信息遗漏；
2. 支持一键保存、二次编辑。

---

# 六、本地AI能力&双模式调度（P0 基础能力）
### 6.1 联网/离线双模式切换
**优先级**：P0
**功能描述**：支持一键切换联网大模型、Ollama本地离线模式，兼顾效果与隐私
**业务落地细则**：联网模式：调用大模型保障文案、任务拆解精度；离线模式：纯本地运算，零网络请求；支持设置默认启动模式
**【前端专项落地细则】**
- `/settings/ai`；Toggle切换；Ollama地址、模型名称输入框
- 模式切换后状态全局同步；连接测试按钮，展示成功/失败提示
**验收标准**：
1. 模式切换流畅无卡顿；
2. 离线模式无任何网络数据上传；
3. 两种模式均可正常执行全量任务；
4. Ollama连通性检测正常。

### 6.2 自然语言混合任务拆解
**优先级**：P0
**功能描述**：一句话识别工作+生活混合需求，自动拆解为标准DAG任务链路
**业务落地细则**：支持复杂混合指令解析；自动区分串行/并行任务；耗时任务并行优化，异常自动重试、缺材提醒
**【前端专项落地细则】**
- 指令输入框（悬浮窗+主面板共用一套组件）；拆解后直接预览ReactFlow DAG，确认后再执行
**验收标准**：
1. 混合任务拆解逻辑合理、链路完整；
2. 并行/串行调度精准；
3. 异常场景自动重试、弹窗提示补全材料；
4. 执行前DAG预览正常。

### 6.3 文本智能处理能力
**优先级**：P0
**功能描述**：支持文本改写、精简、扩写、风格统一、格式规整
**业务落地细则**：适配办公正式风、个人简洁风两种文风；自动清理冗余文字、统一段落格式；支持自定义文本输出偏好
**【前端专项落地细则】**
- 文本输入输出双栏；风格选择下拉；结果一键复制/新建笔记
**验收标准**：
1. 文本处理效果贴合用户偏好；
2. 格式规整、无错乱；
3. 文风自适应迭代生效。

---

# 七、私有化隐私&系统能力（P0 安全底线）
### 7.1 全量数据本地私有化存储
**优先级**：P0
**功能描述**：所有用户数据、记忆、日志、文档、模板全部本地留存，禁止云端上传
**业务落地细则**：任务数据、记账记录、演化日志、向量知识库、个人资料均存储本地user_data目录；无静默上传、无日志外传、无数据泄露
**【前端专项落地细则】**
- 设置页展示当前数据目录；前端不直接读写数据库，全部走IPC调用主进程
**验收标准**：
1. 抓包验证无任何用户数据上传云端；
2. 本地文件完整留存，重启不丢失。

### 7.2 数据备份&恢复&重置
**优先级**：P0
**功能描述**：支持整机数据备份、压缩包恢复、局部/全局数据重置
**业务落地细则**：一键打包所有工作台数据生成备份压缩包；支持导入备份恢复数据；可单独清空演化记忆、记账数据、知识库，或全局重置
**【前端专项落地细则】**
- `/settings/backup`；选择目录弹窗；进度条展示打包/恢复进度；高危重置二次确认
**验收标准**：
1. 备份打包完整无缺失；
2. 恢复后数据完全还原；
3. 重置功能精准，不污染其他数据。

### 7.3 沙箱安全隔离机制
**优先级**：P0
**功能描述**：隔离AI自动生成脚本的运行权限，杜绝本地文件篡改、安全风险
**业务落地细则**：所有自主生成工具、脚本仅在沙箱内运行；限制跨目录读写、网络请求权限；异常脚本自动拦截、日志记录
**【前端专项落地细则】**
- 工具运行日志面板；拦截警告用Tailwind警示色展示
**验收标准**：
1. 沙箱隔离生效，无越权操作；
2. 危险脚本自动拦截，无安全隐患；
3. 运行日志完整可查。

### 7.4 基础系统配置能力
**优先级**：P0
**功能描述**：支持提醒规则、演化参数自定义配置
**业务落地细则**：可视化配置面板，支持开关自定义、参数调节；所有配置本地保存，重启生效
**【前端专项落地细则】**
- 统一`/settings`路由，分类Tab（AI/演化/提醒/备份/外观）；表单受控，实时保存
**验收标准**：
1. 所有配置项可正常修改保存；
2. 配置重启不丢失；
3. 功能生效精准无偏差。

---

# 八、P2中长期规划（预留接口，V1不开发，前端预留路由/组件占位）
1. 学习备考模块：艾宾浩斯复习、题库错题本、考点自动提炼
2. 轻量个人CRM：联系人归档、跟进记录、周期提醒
3. 多媒体资产库：图片语义检索、音视频转录纪要
4. 插件生态：自定义本地插件加载、工具市场扩展
5. 生产力数据报表：工作效率趋势、流程复用率、时间消耗分析
6. 本地日历双向同步、剪贴板智能识别、文件夹监听自动归档

## 附加：前端技术约束（可直接放进PRD技术附录）
1. ✅ 框架：React 18 + Vite + TailwindCSS（JIT）
2. ✅ 状态：Zustand（轻量全局）+ React Hook Form（表单）
3. ✅ 可视化：ReactFlow（DAG）、Recharts（统计图表）、Tanstack Table（表格）
4. ✅ Markdown：react-markdown
5. ✅ UI基础：HeadlessUI 做弹窗/下拉/开关，**不引入AntD/MUI等重型组件库**，保持Tailwind原生可控
6. ✅ 打包：Electron + Vite插件打包EXE；渲染层和主进程IPC通信，文件IO、SQLite、Ollama通信、托盘、定时逻辑全部放**Electron主进程**，React渲染层只负责UI交互
7. ✅ 性能约束：长列表一律虚拟滚动；大计算用useMemo/useCallback；避免全局频繁重渲染

如果你想要，我可以再输出一份：
- 【前端路由清单 + 组件树文档】
- 或者【SQLite表结构设计】
- 或者【Electron主进程 ↔ React渲染层 IPC接口清单】