# 桌面智能助手\-架构设计\+目录结构\+数据库设计\+UI交互原型规范（AI开发终版）

# 第一部分：系统架构 \+ 项目目录拆分文档

## 1\. 整体系统架构（五层分层架构）

本系统采用**分层解耦架构**，完全适配 LangGraph 任务流、自演化闭环、本地私有化部署，满足 EXE 单文件打包、后台常驻、离线/联网双模式。

### 1\.1 五层架构总览

1. **GUI 交互层（PyQt6）**

    - 主窗口、看板、进化中心、知识库页面渲染

    - 托盘后台、悬浮输入框、弹窗提醒、进度可视化 DAG 图

    - 用户指令输入、结果展示、页面切换交互

2. **应用服务层**

    - 定时任务服务、文件管理服务、文书服务、数据台账服务、生活管理服务

    - 模式切换服务（联网/离线 Ollama）

    - 开机自启、托盘常驻、窗口生命周期管理

3. **Agent 内核层（核心）**

    - LangGraph DAG 动态任务调度

    - 自然语言任务拆解、多任务并行/串行排布、异常重试、断点续跑

    - 任务执行队列管理、进度回调

4. **自演化智能层（产品壁垒）**

    - 演化裁判打分系统（工作/生活双维度）

    - 流程优化引擎、记忆权重迭代、模板自动固化、工具自动生成沙箱

    - 用户行为采集与偏好修正

5. **本地私有化存储层**

    - SQLite 结构化数据存储（任务、日程、记账、演化日志）

    - Chroma 向量知识库（本地文档记忆）

    - JSON 配置文件、模板文件、用户习惯权重文件

    - 本地文件归档目录（PDF/Excel/票据/资料）

### 1\.2 核心数据流（固定闭环）

用户输入 → GUI接收 → Agent拆解任务DAG → 工具集群执行 → 结果返回UI → 双维度自检打分 → 自演化模块复盘优化 → 权重/流程/模板固化入库 → 下次执行自动使用新版本能力

### 1\.3 双模式运行机制

- **联网模式**：调用外部大模型，文案质量高、理解复杂混合任务强

- **离线私密模式**：调用本地 Ollama，**零网络上传**，纯本地运算

- 所有用户数据、记忆、演化记录**永远本地存储，无云端同步**

## 2\. 完整项目目录结构（AI严格遵照此结构编码）

适配 PyInstaller 单 EXE 打包、结构干净、无耦合、可长期迭代

```Plain Text
DesktopAssistant/
├── main.py                     # 程序入口、托盘启动、全局初始化
├── config/                     # 全局配置
│   ├── settings.py             # 模式切换、开机自启、提醒配置
│   ├── path_config.py          # 所有本地存储路径统一管理
│   └── app_const.py            # 常量、打分权重、演化阈值
├── gui/                        # PyQt6 界面层
│   ├── main_window.py          # 主窗口、四页面框架
│   ├── page_chat.py            # 对话主窗口 + DAG进度展示
│   ├── page_dashboard.py      # 日程看板、待办、健康提醒
│   ├── page_evolution.py       # 进化中心、迭代记录、权重变化
│   ├── page_kb.py              # 知识库上传、分类、检索
│   ├── tray_icon.py            # 托盘后台、右键菜单
│   ├── float_input.py          # 悬浮快捷输入框
│   └── ui_utils.py             # 弹窗、样式、通用UI组件
├── agent_core/                 # LangGraph 调度内核
│   ├── graph_builder.py        # DAG动态任务图构建
│   ├── task_parser.py          # 自然语言任务拆解
│   ├── task_scheduler.py       # 并行/串行调度、重试、断点续跑
│   └── node_executor.py        # 各任务节点执行器
├── evolution_core/             # 自演化核心模块
│   ├── judge_score.py          # 工作/生活双维度打分
│   ├── flow_optimize.py        # 流程冗余步骤精简
│   ├── weight_evolve.py        # 用户记忆权重迭代
│   ├── template_save.py        # 高频模板自动固化
│   └── sandbox_tool_gen.py     # 安全沙箱、自定义工具生成
├── service/                    # 业务服务层
│   ├── work_service.py         # 办公：周报、台账、文件归档、报销
│   ├── life_service.py         # 生活：记账、日程、采购、家务
│   ├── health_service.py       # 久坐、喝水、作息、睡眠统计
│   ├── schedule_service.py     # 定时任务推送、每日归档、月末汇总
│   └── file_service.py         # PDF/Excel批量处理、归类
├── memory_store/               # 本地存储层
│   ├── sqlite_db.py            # 结构化数据库操作
│   ├── chroma_kb.py            # 向量知识库管理
│   ├── user_weight.py          # 用户习惯权重读写
│   └── file_archive.py         # 本地文件归档管理
├── tools/                      # 工具库
│   ├── excel_tools.py
│   ├── pdf_tools.py
│   ├── text_writer.py
│   ├── bill_stat.py
│   └── sandbox_run.py
├── resources/                  # 静态资源、图标
├── user_data/                  # 用户本地私有数据（打包后保留）
│   ├── db/
│   ├── chroma/
│   ├── templates/
│   ├── logs/
│   └── archive/
└── build/                      # 打包配置
    └── build_spec.py

```

---

# 第二部分：本地存储数据表设计文档（完整可落地）

存储分为三部分：**SQLite结构化数据表 \+ JSON配置/权重表 \+ Chroma向量知识库**，全部本地私有化、不上传云端。

## 1\. SQLite 核心数据表设计

### 1\.1 用户任务任务表 task\_list

存储所有工作、生活、定时任务记录，用于复盘演化

|字段名|类型|说明|
|---|---|---|
|id|int 主键|自增ID|
|task\_type|str|work / life / mix|
|task\_content|text|用户原始指令|
|task\_steps|text|拆解后的DAG步骤JSON|
|status|str|running / success / fail|
|cost\_time|float|执行耗时|
|work\_score|float|工作维度得分 0\-100|
|life\_score|float|生活维度得分 0\-100|
|user\_modify|text|用户手动修改记录|
|create\_time|datetime|创建时间|

### 1\.2 用户习惯权重表 user\_habit\_weight

自演化核心，用于权重升降、偏好记忆

|字段名|类型|说明|
|---|---|---|
|habit\_key|str 主键|习惯唯一标识（如：周三周报、午休1小时）|
|weight|float|权重 0\-10|
|freq\_count|int|触发频次|
|last\_use\_time|datetime|最后使用时间|
|is\_valid|int|是否有效，过期自动降权|

### 1\.3 演化优化日志表 evolution\_log

进化中心展示数据源

|字段名|类型|说明|
|---|---|---|
|id|int 主键||
|evo\_type|str|flow/weight/template/tool|
|before\_content|text|优化前|
|after\_content|text|优化后|
|evo\_time|datetime|优化时间|

### 1\.4 自定义模板表 custom\_template

存储用户高频混合任务一键模板

|字段名|类型|说明|
|---|---|---|
|id|int||
|name|str|模板名称|
|task\_flow\_json|text|完整任务链路JSON|
|create\_time|datetime|生成时间|

### 1\.5 日程待办表 daily\_schedule

### 1\.6 收支记账表 bill\_record

### 1\.7 知识库文件索引表 kb\_file\_index

## 2\. 本地 JSON 配置存储

- **user\_config\.json**：联网/离线模式、自启开关、提醒阈值

- **user\_style\.json**：用户文风偏好、报表简洁度偏好

- **sandbox\_tool\_list\.json**：AI自动生成的专属工具清单

## 3\. Chroma 向量知识库规范

- 分区存储：工作文档 / 合同票据 / 个人笔记 / 生活资料

- 切片规则：512字符切片、128重叠

- 检索模式：混合相似度\+权重时间衰减

- 全部向量数据本地 user\_data/chroma 存放

---

# 第三部分：界面原型 \+ 完整交互说明（AI可直接写UI代码）

共 4 大主页面 \+ 托盘 \+ 悬浮窗 \+ 弹窗体系，下面是**逐页面标准原型描述\+交互逻辑**，无需图片，AI可直接复刻界面。

## 1\. 全局窗口基础规范

- 主窗口尺寸：默认 1000×700，可缩放

- 整体风格：极简浅色办公风、低饱和度、清爽无花哨动画

- 顶部 Tab 栏切换四大页面：对话、看板、进化中心、知识库

- 关闭窗口最小化至托盘，不退出程序

## 2\. 页面一：对话主窗口（核心交互页）

### 布局结构

1. **左侧：任务DAG流程图区域**

    - 可视化展示当前任务拆解链路、执行节点、已完成/进行中/未执行状态

    - 自动刷新执行进度

2. **右侧：对话日志区域**

    - 上方历史对话记录

    - 下方输入框：支持多行自然语言混合任务输入

### 交互逻辑

- 用户输入指令 → 自动生成 DAG 流程图

- 执行中实时打印日志、节点亮起

- 失败节点标红、支持重试

- 任务结束展示自检得分、优化小结

## 3\. 页面二：日程看板（Dashboard）

### 布局结构

- 左上：今日工作清单（可勾选完成）

- 右上：今日生活待办、作息计划

- 下方：健康提醒卡片（久坐、喝水、睡眠统计）

### 交互逻辑

- 早8点自动刷新今日清单

- 待办支持手动增删、标记完成

- 久坐、喝水弹窗定时弹出、可延后/关闭

## 4\. 页面三：进化中心（产品核心特色页面）

### 布局结构

- 顶部：总体进化统计（优化次数、新增工具数、模板数量）

- 中部：时间轴列表，逐条展示迭代记录

- 每条记录展示：优化类型、优化前/后对比、执行时间

- 底部：用户记忆权重可视化展示（高低权重习惯展示）

### 交互逻辑

- 支持按：流程优化/工具新增/模板固化/权重迭代 筛选

- 可查看历史所有版本变化

## 5\. 页面四：知识库管理

### 布局结构

- 左侧：分类树（工作文档、合同票据、个人资料、笔记、账单）

- 右侧：文件列表 \+ 上传按钮

### 交互逻辑

- 支持拖拽上传、批量上传

- 上传自动向量化、归入对应分类

- 支持检索、预览、本地打开

## 6\. 系统常驻能力：托盘菜单

右下角托盘右键菜单：

- 打开主界面

- 快速新建待办

- 切换联网/离线模式

- 开机自启设置

- 退出程序

## 7\. 悬浮快捷输入框

- 可全局悬浮、置顶、可拖动

- 一键输入指令、快速提交任务

- 轻量化，不遮挡窗口

## 8\. 弹窗交互规范

- 任务报错弹窗：提示原因 \+ 手动重试/补充文件按钮

- 定时提醒弹窗：久坐、喝水、日程提醒

- 演化完成轻提示：本次优化内容极简提示
